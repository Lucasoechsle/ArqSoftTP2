/**
 * ProductsCastrolController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */
/*
module.exports = {
  login: async function (req, res) {
    const cant = req.param('cantidad');
    const name = req.param('producto');
    const code = req.param('codigo');

    const Productos = await Productos.findOne({
      cant: cantidad,
      name: producto,
    });
    if (producto) {
      req.session.producto = producto;
      res.redirect("/Carrito/add");

    } else {
      req.session.user = null;
      res.redirect("/ProductsCastrol");

    }
  },


};

 */

